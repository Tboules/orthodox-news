query CategoryArticles($slug: String) {
  category(where: { slug: $slug }) {
    slug
    articles {
      id
      slug
      title
    }
    subCategories {
      name
      slug
      arabicDisplay
    }
  }
}
